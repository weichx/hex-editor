export enum CommandType {
    Create,
    Destroy,

    SetRect,
    SetImage,
    SetPosition,
    SetDimensions,
    SetTransform,
    SetParent,

    PaintBackground,
    SetText,
    UpdateInput
}