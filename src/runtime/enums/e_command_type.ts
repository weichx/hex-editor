export enum CommandType {
    Create,
    Destroy,
    Update,
    Move,
    Replace,
    SetRect,
    SetImage,

    SetPosition,
    SetDimensions,
    SetTransform,
    SetParent,

    PaintBackground,
    SetText,
    SetFont,
    UpdateInput
}