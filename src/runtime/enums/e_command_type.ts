export enum CommandType {
    Create,
    Destroy,
    Update,
    Move,
    Replace,
    SetRect,

    SetPosition,
    SetDimensions,
    SetTransform,
    SetParent,

    PaintBackground,
    SetText,
    SetFont,
    UpdateInput
}